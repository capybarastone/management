# Herd Management UI

## Run
(After installing and starting the backend server)
1. Install `Node.JS` and `npm`
2. Run `npm install`
3. Copy `.env.example` to `.env` and edit it
4. Run `./run.sh` to start server
